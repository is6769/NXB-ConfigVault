spring:
  cloud:
    gateway:
      routes:
        - id: CRM-service
          uri: lb://CRM-service
          predicates:
            - Path=/crm/**
          filters:
            - StripPrefix=1
            - JwtHeaderFilter

const:
  auth:
    introspection-uri: http://localhost:8765/auth/introspection

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true

server:
  port: 8080
