eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka
  instance:
    prefer-ip-address: true

const:
  brt-service:
    BASE_URL: http://BRT-service

server:
  port: 8083